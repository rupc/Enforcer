package util_test

// func TestReadPeerList(t *testing.T) {
// path := "/home/jyr/go/src/github.com/hyperledger/fabric/sampleconfig/peerlist"
// tbl := audit.ReadPeerList(path, 10)
// for k, v := range tbl {
// fmt.Printf("%d %s\n", k, v)
// }
// }

// func TestGetQuorumSizeFromTotal(t *testing.T) {
// assert := assert.New(t)
// a := audit.GetQuorumSizeFromTotal(4)
// assert.EqualValues(3, a, "Quorum size of 4 should equal to 3")

// b := audit.GetQuorumSizeFromTotal(5)
// assert.EqualValues(3, b, "Quorum size of 5 should equal to 3")

// c := audit.GetQuorumSizeFromTotal(6)
// assert.EqualValues(3, c, "Quorum size of 6 should equal to 3")

// d := audit.GetQuorumSizeFromTotal(7)
// assert.EqualValues(5, d, "Quorum size of 7 should equal to 5")

// e := audit.GetQuorumSizeFromTotal(10)
// assert.EqualValues(7, e, "Quorum size of 10 should equal to 7")
// }
